#!/usr/bin/env bash

image_tag="biopod/autodock:4.2.6"

echo "Starting ${image_tag} container in current dir -> $(pwd)"
docker run --rm -it \
       --mount type=bind,source=$(realpath $(pwd)),target=/mnt \
       --user $(id -u):$(id -g) \
       ${image_tag}

